project:
  seed: 1234

paths:
  base_dir: data
  raw_dir: raw/m4singer
  processed_dir: processed
  manifest_dir: manifests

audio:
  sample_rate: 16000

encoder:
  name: hubert_soft

pitch:
  name: rmvpe
  backend: auto

device:
  torch: auto
  mlx: auto

preprocess:
  min_seconds: 1.0
  slice_on_silence: false
  num_workers: 0
  torch_threads_per_worker: 1
